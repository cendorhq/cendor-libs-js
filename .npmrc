# ESM-only workspace. Keep peer deps explicit; do not auto-install them.
auto-install-peers=false
strict-peer-dependencies=false
# Provenance is controlled by NPM_CONFIG_PROVENANCE in release.yml — currently "false" because
# these repos are private (sigstore rejects private sources with E422); flip it to "true" once public.
