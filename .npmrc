# ESM-only workspace. Keep peer deps explicit; do not auto-install them.
auto-install-peers=false
strict-peer-dependencies=false
# Provenance is set per-publish in CI via `npm publish --provenance` (OIDC trusted publishing).
